<h1>Editing <%= @song.title %></h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :artist %>
  <%= f.collection_select(:artist_id, @artists,:id, :name, prompt:true) %>
  <br />

  <%= f.submit %>
<% end %>


